#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

int main(int argc, char const *argv[])
{
  const char *temp = argv[1];
  printf("%s\n", temp);
  // // char temp[2];
  // // temp[0] = '1';
  // // temp[1] = '0';
  // if (isdigit(argv[1]) != 0)
  // {
  //   int x = atoi(argv[1]);
  //   printf("%d\n", x);
  // }
  return 0;
}
